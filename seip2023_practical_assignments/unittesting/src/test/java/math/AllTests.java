package math;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({
        FactorialParameterizedTest.class,
        MyMathTest.class
})
public class AllTests {
    // This class doesn't have any code as it
}